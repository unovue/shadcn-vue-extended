# Implementation Plans

Generated by the improve skill on 2026-07-20 (audit at commit `db64bb8`), focused
on making shadcn-vue-extended ready for developers to **consume** (install
registry items) and **contribute** (docs, workflow, CI). The session ran
non-interactively, so plans were written for the top 5 findings by leverage —
the default described in the skill.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Write a real README and Getting Started doc | P1 | S | — | DONE (approved; commit b0665a1 on `advisor/001-readme-and-getting-started`, worktree `.claude/worktrees/agent-a86b080ab30fcdf27`) |
| 002 | Add CONTRIBUTING.md, .env.example, PR/issue templates | P1 | S | — | DONE (approved; commit 43e41a9 on `advisor/002-contributing-guide`, worktree `.claude/worktrees/agent-ab3d88c4268757c8c`) |
| 003 | Make `pnpm lint` pass on a clean checkout | P1 | S | — | DONE (approved; commit e3428dc on `advisor/003-fix-lint-baseline`, worktree `.claude/worktrees/agent-afd2270a62bc40420`) |
| 004 | Add CI: lint, registry drift check, site build | P1 | M | 003, 005 | DONE (approved; commit eca6027 on `advisor/004-ci-workflow`, worktree `.claude/worktrees/agent-a72b84a681f063cd0`. Branch contains 003 + cherry-picked 005 commits + CI. Drift check hardened to `git status --porcelain` after upstream CLI output drift hit the original `git diff` form) |
| 006 | AutoForm enhancement program (all 19 open issues; 5 phases) | P1 | L | — | IN PROGRESS (program; phases split below; work lands on `feat/autoform`) |
| 007 | AutoForm Phase 0: vitest harness + characterization tests | P1 | M | — | DONE (approved; commits c6a14a4 + 23f21f8 merged into `feat/autoform`; 63 tests; also added missing calendar/radio-group/switch/native-select primitives. Tracking PR: #20) |
| 009 | AutoForm Phase 2: dependencies overhaul (#2 #14 #15) | P1 | M | 008 | DONE (approved; c941ff6 merged, artifacts @ 1eafcba, 80 tests. REQUIRES now drives validation via a TypedSchema.parse wrapper — executor rejected the planned superRefine approach with traced evidence [it breaks vee-validate default extraction]. #15: analyzed as already SSR-deterministic in-scope; determinism regression tests added, issue likely needs a Nuxt-side repro) |
| 008 | AutoForm Phase 1: seven bug fixes (2 parallel batches) | P1 | M | 007 | DONE (approved; all 7 fixed — #10 #12 #8 #4 #13 #5 #3 — merged to `feat/autoform` @ 42154f5, artifacts rebuilt @ 3639cf3, 73 tests green. Notable: #5's real root cause was a duplicate `useField` registration [fixed standalone:true], #3's was missing options extraction in array itemShape — both plan hypotheses corrected by executor evidence) |
| 005 | Registry metadata, Supabase env docs, block install paths | P2 | M | — | DONE (approved; commits 7134f8e + cbee554 on `advisor/005-registry-metadata`, worktree `.claude/worktrees/agent-a8363a7a0980b8b4f`. Deviation approved: no `target` on dialog files — empirically, `target` bypasses the `@/components` alias; `registry:component` without target installs correctly) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 004 requires 003: CI's lint job gates on a green lint baseline (currently 22 errors).
- 001 and 003 overlap on `README.md` whitespace — either order works; 003's Step 3 is a no-op on that file if 001 landed first.
- 005 regenerates `registry.json` / `public/r/*.json`; land it before (or rebase it on) any other registry change, and update CONTRIBUTING (002) in the same PR if the block file-type convention changes.

## Findings considered and rejected / deferred

- **No tests or typecheck script**: deferred — this is a docs/registry site with little logic; the CI `build` job (004) already compiles every registry component via Nuxt auto-import. Add `nuxt typecheck` later if component complexity grows.
- **D1 `database_id` hardcoded in `nuxt.config.ts`**: not blocking — it only affects deploying the canonical site; forks can swap the id. Local `pnpm dev` uses local storage, not D1.
- **`build:registry` uses `shadcn-vue@latest` (unpinned)**: deferred one-liner, documented in plan 004's maintenance notes — pin only if the CI drift job flakes on an upstream release.
- **AutoForm docs lack live previews**: content authoring, not a defect — noted in plan 005's maintenance notes as a good-first-issue.
- **Lint errors in `public/r/*.json` / `registry.json`**: not "fixed" in the generator — generated files are eslint-ignored instead (plan 003).

## New findings surfaced during execution (follow-ups, not yet planned)

- ~~**`supabase-client` installs to a nested path**~~: OBSOLETE — all Supabase items were removed on 2026-07-20 (commit 427308f) in favor of official Supabase UI Vue/Nuxt support.
- **AutoForm array removal is a no-op for non-last items** (found during plan 007 characterization; worse than issue #5's ghost-entry symptom): only removing the last item reliably shrinks the list. Fold into plan 008 Batch B4.
- **AutoForm file field never binds `componentField`** (found during plan 007): `AutoFormFieldFile.vue` doesn't spread `slotProps.componentField` onto its input, unlike every other field — likely means file values never reach the form model. Verify and fix in a later phase (candidate for 008 follow-up or Phase 3).
- **`shadcn-vue@latest` output format drifted** (type-key position + new `public/r/registry.json` index): absorbed by plan 005's regeneration; consider pinning the CLI version in `scripts/build-registry.ts` if the CI drift job flakes again.
